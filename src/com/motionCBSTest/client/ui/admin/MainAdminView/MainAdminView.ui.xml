<ui:UiBinder xmlns:ui='urn:ui:com.google.gwt.uibinder'
             xmlns:g='urn:import:com.google.gwt.user.client.ui'>
    <ui:style>

        /*.outerPanel {
            display: flex;
            width: 100%;
            background-color: #E6E8EC;
            justify-content: center;
            align-items: center;
        }*/


        .btn {
            height: 40px;
            width: 100%;
            border: none;
            border-radius: 2px;
            font-size: 18px;
            background-color: #333;
            cursor: pointer;
            cursor: hand;
            color: #fff;
            font-size: 14px;
            margin-bottom: 10px;
        }

        .btn:hover {
            background-color: #fff;
            color: #333;
        }

        .menu {
            padding: 10px;
            background-color: #E6E8EC;
        }

    </ui:style>

    <g:HTMLPanel>
        <g:DockLayoutPanel unit="EM" width="100%" height="100%">

            <g:west size="10">
                <g:FlowPanel styleName="{style.menu}">
                    <g:Button text="Ændr træner oplysninger" styleName="{style.btn}" ui:field="changeBtn"/>
                    <g:Button text="Godkend/Slet trænere" styleName="{style.btn}" ui:field="trainerStatusBtn"/>
                    <g:Button text="Vis oplysninger om trænere" styleName="{style.btn}" ui:field="showInfoBtn"/>
                    <g:Button text="Træner oplysninger efter holdtype" styleName="{style.btn}"/>
                    <g:Button text="Vis træner- og holdliste" styleName="{style.btn}"/>
                    <g:Button text="Statistik" styleName="{style.btn}"/>
                    <g:Button text="Log ud" styleName="{style.btn}" ui:field="logoutBtn"/>
                </g:FlowPanel>
            </g:west>

            <g:center>
                <g:DeckLayoutPanel ui:field="centerPanel"/>
            </g:center>

        </g:DockLayoutPanel>

    </g:HTMLPanel>
</ui:UiBinder>